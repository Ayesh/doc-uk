name: "Build Ukrainian language documentation - 2"

on:
  push:
  pull_request:
    branches: "master"
  workflow_dispatch:

jobs:
  build:
    uses: Ayesh/doc-base/.github/workflows/reusable.yml@master
    with:
      language: 'uk'
